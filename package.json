{
  "name": "fotobooth-lan",
  "version": "1.0.0",
  "description": "Photo booth LAN server para eventos",
  "main": "server.js",
  "scripts": {
    "client-build": "cd client && npm install && npm run build",
    "server": "nodemon server.js",
    "dev": "concurrently \"npm run client-build\" \"npm run server\""
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "nodemon": "^3.1.14",
    "qrcode": "^1.5.3",
    "selfsigned": "^2.4.1",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
